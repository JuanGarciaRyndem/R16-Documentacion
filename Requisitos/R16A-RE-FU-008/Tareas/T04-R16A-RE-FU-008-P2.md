# [ R16A-RE-FU-008 ] [UPDATE-TABL-CH] Ampliar modelo EF6: entidades MailbotEventoCorreo y MailbotClasificacionLog

---

## Aplicativos

- ProquifaDotNet

## Módulos

- Mailbot
- Buzones

## Consideraciones previas

- Las tareas T02 y T03 deben estar ejecutadas (las tablas deben existir en BD).
- El proyecto `_Data\Mail` usa Entity Framework 6 con modelo `.edmx` o Code-First. Verificar el patrón utilizado en `ProquifaDotNetModel.cs`.
- No modificar entidades existentes.

## Objetivo general

Agregar las entidades `MailbotEventoCorreo` y `MailbotClasificacionLog` al modelo de Entity Framework 6 del proyecto `_Data\Mail` para que el repositorio ProquifaDotNet pueda consultar y escribir en estas tablas.

## Objetivos específicos

1. Crear la clase de entidad `MailbotEventoCorreo` en `_Data\Mail\ProquifaDotNetContext\` con todas las propiedades que mapean a las columnas de la tabla.
2. Crear la clase de entidad `MailbotClasificacionLog` en `_Data\Mail\ProquifaDotNetContext\` con todas las propiedades.
3. Agregar `DbSet<MailbotEventoCorreo>` y `DbSet<MailbotClasificacionLog>` al contexto en `ProquifaDotNetModel.Context.cs`.
4. Configurar mappings (TableAttribute o Fluent API) según el patrón existente en el proyecto.

## Resultado esperado

- El contexto EF6 de `_Data\Mail` puede realizar operaciones CRUD sobre las tablas `MailbotEventoCorreo` y `MailbotClasificacionLog`.
- El proyecto compila sin errores.

## Entregables

- Archivos de entidades nuevos (o modificados si es edmx).
- Contexto actualizado con DbSets.

## Criterios de aceptación

- [ ] El proyecto `Logic.Pqf.Mail` compila sin errores tras los cambios.
- [ ] Se puede instanciar el contexto y consultar ambas tablas (prueba manual o unitaria).
- [ ] Las propiedades de navegación (FK a Region, MailbotEventoCorreo, CorreoRecibido) están configuradas correctamente.
- [ ] No se rompen entidades ni DbSets existentes.

## Más información de la tarea

- Referencia: `R16A-RE-FU-008-P2-Back.md` — PARTE 1, sección 1.2 (Capa `_Data\Mail`)
- Proyecto: `_Data\Mail\Logic.Pqf.Mail.csproj`

## Recursos

- Repositorio: ProquifaDotNet, branch `develop-pack04`
- Proyecto: `_Data\Mail\`
