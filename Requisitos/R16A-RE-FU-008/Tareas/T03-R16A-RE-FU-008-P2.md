# [ R16A-RE-FU-008 ] [CREATE-TABL-CH] Script BD: CREATE TABLE MailbotClasificacionLog

---

## Aplicativos

- ProquifaDotNet (Base de Datos — SQL Server RYNL010)

## Módulos

- Mailbot

## Consideraciones previas

- La tarea T02 debe estar ejecutada previamente (la tabla referencia `MailbotEventoCorreo` vía FK).
- También referencia `CorreoRecibido` (FK nullable).

## Objetivo general

Crear la tabla `MailbotClasificacionLog` para auditoría de clasificaciones del Agente IA, permitiendo reentrenamiento y análisis de precisión.

## Objetivos específicos

1. Crear tabla `MailbotClasificacionLog` con las columnas:
   - `IdMailbotClasificacionLog` (PK, uniqueidentifier, DEFAULT NEWID())
   - `IdMailbotEventoCorreo` (FK → MailbotEventoCorreo, NOT NULL)
   - `IdCorreoRecibido` (FK → CorreoRecibido, NULL)
   - `ClasificacionIA` (varchar 150, NOT NULL)
   - `Confianza` (decimal 5,2, NULL)
   - `ClasificacionFinal` (varchar 150, NULL)
   - `EsCorrecta` (bit, NULL)
   - `ModeloVersion` (varchar 50, NULL)
   - `PromptTokens` (int, NULL)
   - `CompletionTokens` (int, NULL)
   - `FechaRegistro` (datetime, DEFAULT GETDATE())
   - `Activo` (bit, DEFAULT 1)

## Resultado esperado

- Tabla `MailbotClasificacionLog` creada con FKs válidas hacia `MailbotEventoCorreo` y `CorreoRecibido`.

## Entregables

- Script SQL de creación (idempotente).
- Script SQL de rollback (DROP TABLE).

## Criterios de aceptación

- [ ] El script se ejecuta sin errores en RYNL010.
- [ ] La FK a `MailbotEventoCorreo` es válida.
- [ ] La FK a `CorreoRecibido` es nullable y válida.
- [ ] El script de rollback elimina la tabla correctamente.

## Más información de la tarea

- Referencia: `R16A-RE-FU-008-v2_Propuesta2.md` — sección "Tablas BD Nuevas Requeridas > MailbotClasificacionLog"

## Recursos

- Servidor: RYNL010
- Base de datos: ProquifaDotNet
